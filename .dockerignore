# Build the image from clean source — never copy host build output or installed
# deps (their native binaries are built for the host arch and would not match the
# image).
**/node_modules
**/dist
**/coverage
.git
.workaholic
**/*.log
