# plgg — values & effects

::: info Page stub
This page's content is authored in **T3 — plgg core:
values & effects**. T1 reserves its place in the
information architecture.
:::

This page will cover plgg's value primitives and the
effect-composition tools: `Option`, `Result`, and the
`pipe` / `cast` / `proc` / `flow` pipeline operators.
