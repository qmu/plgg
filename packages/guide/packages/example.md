# example (tutorial)

::: info Page stub
This page's content is authored in **T6 — plgg-view**
(the SSR + CSR walkthrough). T1 reserves its place in
the information architecture.
:::

`example` is the family's end-to-end demo: one
Elm-Architecture program (`Model` / `update` / `view`)
rendered both server-side (SSR via plgg-server) and
client-side (CSR via plgg-view's runtime). This page
will walk through it as the guide's capstone tutorial.
