# plgg-server

::: info Page stub
This page's content is authored in **T5 — the HTTP
stack**. T1 reserves its place in the information
architecture.
:::

`plgg-server` is a server-side web router and HTTP
handler built from scratch on plgg: a
pipeline-composed `Web`, a `node:http` adapter, and
SSR via `pageResponse`. It consumes
[plgg-http](/packages/plgg-http)'s model.
