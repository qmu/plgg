# The seeded, agent-editable COPY of the guide corpus
# (locked design decision: the agent edits this copy, so
# the real packages/guide never accumulates uncommitted AI
# edits; `npm run reset-content` re-seeds it).
content/
