// CLI entrypoint placeholder. The `new`/`up`/`down`/
// `status` (+ `--dry-run`) dispatch is filled in by the
// CLI ticket; for now it exits cleanly so the `bin/`
// launcher is runnable from the scaffold onward.
process.stdout.write(
  "plgg-db-migration: CLI not yet implemented\n",
);

export {};
