Update/Add comments on every files in src/plgg to catch up with latest implementations.

* First check changed files on this branch, read `.ts` files in src/plgg.
* Supply JSDoc style comments to every exporting members starting from `/**` to multi lines.
* Comment concisely and clearly, avoid more than 3 lines description.
* Tags like `@param`, `@example` are not required, erase if they exist.
