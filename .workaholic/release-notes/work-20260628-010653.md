# Remove unused devDeps and add dependabot.yml to unblock security updates etc

## Summary

A Config/housekeeping pair: shed two unused devDeps (`dotenv`, `happy-dom`) to reinforce the repo's vendor-neutrality posture, and add `.github/dependabot.yml` to clear a failing Dependabot security-update job. No runtime source changed.

## Key Changes

- Removed unused `dotenv` (plgg-foundry/plgg-kit) and `happy-dom` (plgg-server) devDeps, regenerating the affected lockfiles
- Rewrote the plgg-kit README env example to Node-native `process.loadEnvFile()` / `--env-file`
- Added `.github/dependabot.yml` (explicit npm ecosystem over `/packages/*`) to short-circuit the `private_registry_config_not_found` abort and unblock the happy-dom security advisory

## Changes

### Added
- `.github/dependabot.yml` configuring the npm ecosystem across all `packages/*` workspaces (weekly), enabling security and version update PRs

### Removed
- Unused `dotenv` devDep from plgg-foundry and plgg-kit
- Unused `happy-dom` devDep from plgg-server (retained where genuinely used: plgg-view, plgg-test, example)

## Metrics

- **Tickets Completed**: 2

## Links

- [Pull Request](https://github.com/qmu/plgg/pull/49)
- [Branch Story](.workaholic/stories/work-20260628-010653.md)
