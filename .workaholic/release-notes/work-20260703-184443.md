# Harden the guide dev container, absorb plggmatic into plggpress, and lead Vocabulary docs with examples

## Summary

Made `serve-guide.sh` work from a git worktree by fixing `build.sh`'s repo-root resolution, then fixed the two failures that unmasked (missing `plgg-cli`, drifting provisioning lists) and added a guard against recurrence. Absorbed the `plggmatic` framework — now developed in its own repository — into `plggpress`, making it self-contained on the plgg family. Reorganized all 12 app-facing guide Vocabulary articles to lead with a runnable `pipe` example before any rationale.

## Key Changes

- Fixed `scripts/build.sh` to resolve the repo root from the script path, so the guide dev container builds from a git worktree (where `git rev-parse` fails inside the mount)
- Provisioned `plgg-cli` in the guide container and added `gate-guide-deps.sh`, guarding the entrypoint-install / compose-volume / build.sh lists against drift
- Absorbed `plggmatic` into `plggpress/src/framework/` and removed `packages/plggmatic`; plggpress now depends on the plgg family directly
- Reorganized 12 Vocabulary guide articles example-first and recorded the page-structure convention
- Dropped the dead `git` install from the guide Dockerfile and switched `serve-guide.sh` to detached

## Changes

### Added

- `scripts/gate-guide-deps.sh` — a check-all gate asserting the guide container's three provisioning lists stay consistent and cover plggpress's runtime deps
- `## Writing an app with it` example-first sections across the 12 app-facing Vocabulary articles, each with a `pipe`-based example verified against real package exports

### Changed

- `scripts/build.sh` resolves `REPO_ROOT` via the script path instead of `git rev-parse --show-toplevel` (worktree/container-safe, no-op on the main checkout)
- The guide container provisions `plgg-cli` (entrypoint install loop + compose volume)
- `serve-guide.sh` runs the dev container detached (`up --build -d`) instead of foreground
- `plggpress` absorbs the framework internally (`plggpress/framework`) and gains direct deps on `plgg-http`, `plgg-server`, `plgg-view`, `plgg-md`, `plgg-highlight`, `plgg-cli`
- The internal framework barrel drops the redundant `plgg-http` star export and disambiguates `renderToString` / `collectCss` to plgg-view (native-ESM source loading)
- Guide Vocabulary pages, `contributing/conventions.md`, README, and `site.config.ts` updated for the example-first structure and the plggmatic removal

### Removed

- `packages/plggmatic` and its `scripts/coverage-plggmatic.sh` / `scripts/test-plggmatic.sh` (framework now in its own repository, absorbed by plggpress)
- The `git` apt install from the guide Dockerfile (no longer used after the `build.sh` fix)
- The guide's `plggmatic` sidebar section and `/packages/plggmatic` article

## Metrics

- **Tickets Completed**: 6
