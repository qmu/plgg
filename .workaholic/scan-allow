# Paths that legitimately contain secret-SHAPED content and no secret.
#
# Read by the branch-safety scan (workaholic:release-scan) as :(exclude) globs.
# This is a pre-declaration made in review, not an override: the file is
# committed, so every entry lands in a diff a human reads, and secret findings
# stay non-overridable everywhere else.
#
# Keep every entry as narrow as the file it excuses. A directory glob here would
# silently cover the next file added beside it.

# AWS's PUBLISHED SigV4 test-suite vectors. The "secret access key" in this
# fixture is `wJalrXUtnFEMI/K7MDENG+bPxRfiCYEXAMPLEKEY` — the placeholder AWS
# itself prints in its public documentation and ships in the test suite, paired
# with the access key id `AKIDEXAMPLE`. It authenticates nothing and grants
# nothing; it exists so that a signer can be checked against AWS's own expected
# signatures, which is the entire acceptance criterion of the ticket that added
# it. Any spelling of the vectors trips a credential pattern, because being
# credential-shaped is the point.
packages/plgg-fetch/src/domain/usecase/fixtures/sigv4Vectors.ts
