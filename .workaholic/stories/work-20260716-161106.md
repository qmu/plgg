---
type: Story
branch: work-20260716-161106
tickets_completed: 1
mission: [plggmatic-ai-native-ui-toward-a-dsl]
tickets: [20260716161118-publish-dialect-packages.md]
---

## 1. Overview

A two-line release branch: bump plgg-ir-language and plgg-ir-manifest from 0.0.1 to 0.0.2 so the dialect exports PR #74 landed on main (`manifestDialect`, derived `manifestLanguage`, `mapDialect`, `contextOf`) actually reach the npm registry, where the plggmatic consumer imports them.

**Highlights:**

1. plgg-ir-language 0.0.1 → 0.0.2 (ships `mapDialect` + `contextOf`)
2. plgg-ir-manifest 0.0.1 → 0.0.2 (ships `manifestDialect` + derived `manifestLanguage`)
3. Preflight confirms the publish set is exactly these two packages

## 2. Motivation

PR #74's compose seam exists only in the repo; both packages sit at 0.0.1 on the registry, recorded as the deferred concern `new-exports-are-not-yet-on`. The bump makes `/ship`'s developer-driven npm gate fire, and the publish closes that concern.

## 3. Changes

### 3-1. Bump plgg-ir-language and plgg-ir-manifest to 0.0.2 ([3e486ae4](https://github.com/qmu/plgg/commit/3e486ae4))

Version metadata only — `file:` dependencies mean nothing else in the repo changes; `publish-npm.sh` rewrites them at publish time.

## 4. Outcome

The ship-time npm preflight lists exactly `plgg-ir-language@0.0.2` and `plgg-ir-manifest@0.0.2`; after the developer publish, plggmatic can `npm install` the compose seam.

## 5. Historical Analysis

Same-day follow-through on PR #74's post-release instruction and the extracted concern — the release flow's "publish is a developer-driven pre-merge gate, fired only on a version bump" design working as intended.

## 6. Concerns

None — the standing corpus is unchanged by a version bump; the `new-exports-are-not-yet-on` concern is resolved at this ship's publish gate (evidence recorded pre-merge).

## 7. Successful Development Patterns

- A registry-publish follow-up as its own tiny ticket keeps the publish auditable (ticket → bump → gated developer publish → verified registry state) instead of riding unrelated work.

## 8. Release Preparation

**Verdict**: Ready for release

### 8-1. Concerns

- None — two-line version metadata diff; fresh check-all runs as the readiness proof.

### 8-2. Pre-release Instructions

- The developer runs the npm publish at the ship gate (token/2FA); the agent preflights, awaits, and verifies.

### 8-3. Post-release Instructions

- None.

## Deployment Evidence

- **When:** 2026-07-16T17:33:58+09:00
- **Target:** plgg npm packages
- **Method:** api-probe
- **Status:** pass
- **Observed:** Developer published; verified: npm view resolves plgg-ir-language@0.0.2 and plgg-ir-manifest@0.0.2; scratch-dir npm install plgg-ir-manifest@0.0.2 succeeds and import() exposes manifestDialect/manifestLanguage/mapDialect/compose; file: deps rewritten to caret ranges on the registry
