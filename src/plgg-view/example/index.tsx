/**
 * plgg-view examples — a small gallery of focused `.tsx` components, each
 * demonstrating one capability of authoring views as JSX components on top of
 * the plgg-view runtime. There is no HTML and no DOM here; the result of any
 * of these components is the pure-data `VNode` tree the JSX runtime produces.
 *
 * See `example.spec.tsx` for the runnable assertions that walk the resulting
 * trees. Per-package usage instructions live in `../README.md` (Usage).
 */
export { Greeting } from "./Greeting";
export { Badge } from "./Badge";
export { Card } from "./Card";
export { Menu } from "./Menu";
export { Notice } from "./Notice";
